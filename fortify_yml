stages:
  - fortify_scan

before_script:
  - export PATH=$PATH:/path/to/fortify/bin
  - export PATH=$PATH:/path/to/dotnet/sdk

fortify_scan:
  stage: fortify_scan
  script:
    - ./run_fortify_scan.sh
  only:
    - branches
  variables:
    SSC_URL: $SSC_URL
    FORTIFY_TOKEN: $FORTIFY_TOKEN
